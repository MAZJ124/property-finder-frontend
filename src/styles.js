import React from 'react'
import { styled } from "@mui/system"

export default styled(() => ({
  LoginBtn: {
    backgroundColor: "white",
    color: "black",
    width: "15rem",
    fontSize: "1.1rem"
  }
}))

